This project finds the best supervised machine learning model for two problems, one linear and one categorical.

The linear model looks to explain test scores for students in California.

The categorical model looks to separate red wines from white wines.